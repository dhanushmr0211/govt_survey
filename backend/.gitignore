node_modules
.env
.env.*
npm-debug.log*
.DS_Store
coverage
*.log

# Database dumps
dump.sql

# GCP Service Account Keys - NEVER commit these!
gcp-key.json
*.json
!package.json
!package-lock.json

# Local uploads directory (never commit uploads to Git)
/uploads/

# Ignore local database migration, helper, and test scripts at the root level
/*.js

# Temporary scratch and dev helper files
src/scratch/
test_*.js
*.test.js
*.spec.js
